# Rally

Every Team, Every Game, One Place.

## Local development

Always use `npm run dev:clean` instead of `npm run dev` when starting a new session, to avoid stale server processes serving cached or incomplete data.